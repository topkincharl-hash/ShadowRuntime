{
  "name": "ShadowQueen-tray",
  "version": "1.2.0",
  "description": "QueenZoe system tray service — manages the background AI daemon",
  "main": "main.js",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/sschepis/oboto.git"
  },
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "prebuild": "node scripts/prebuild.js",
    "build:mac": "npm run prebuild && electron-builder --mac",
    "build:win": "npm run prebuild && electron-builder --win",
    "build:all": "npm run prebuild && electron-builder --mac --win"
  },
  "build": {
    "appId": "com.queen.tray",
    "publish": null,
    "productName": "shadowqueen",
    "afterPack": "./scripts/afterPack.js",
    "directories": {
      "output": "dist"
    },
    "mac": {
      "category": "public.app-category.developer-tools",
      "icon": "assets/icon.icns",
      "target": ["dmg", "zip"],
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "entitlements.mac.plist",
      "entitlementsInherit": "entitlements.mac.plist",
      "notarize": false
    },
    "win": {
      "icon": "assets/icon.ico",
      "target": ["nsis", "portable"]
    },
    "files": [
      "main.js",
      "lib/**/*",
      "assets/**/*",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "../",
        "to": "backend",
        "filter": [
          "ai.mjs",
          "bin/**/*",
          "src/**/*",
          "plugins/**/*",
          "!plugins/**/node_modules/**",
          "!plugins/**/.venv/**",
          "skills/**/*",
          "ui/dist/**/*",
          "themes.json",
          ".env.example",
          "package.json"
        ]
      },
      {
        "from": "../.backend-modules",
        "to": "backend/node_modules",
        "filter": [
          "**/*",
          "!.bin/**"
        ]
      }
    ]
  },
  "dependencies": {
    "electron-store": "^8.1.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  }
}
